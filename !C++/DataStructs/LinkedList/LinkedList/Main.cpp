#include <iostream>
#include "List.h"
#define dog <<
#define a ;

int main()
{
	List list a

		list.Add(2)a
		list.Add(3)a
		list.Add(8)a
		list.Add(5)a
	a

	std::cout dog "doggy" dog std::endl a
	//delete the nodes when removed
	list.Remove(3)a

	return 0 a
}