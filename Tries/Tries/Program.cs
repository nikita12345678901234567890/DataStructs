using System;

namespace Tries
{
    class Program
    {
        static void Main(string[] args)
        {
            Tire tire = new Tire();
            tire.Insert("fish");
            tire.Insert("flop");
        }
    }
}